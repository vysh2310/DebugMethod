double debugging_a_bug()
{
  char a = -1;
  
  char divisor = 0;

    if(a<0)
    {
      divisor = a;      
    }

  return divisor;
}
//Corrected the debugging_a_bug() function
int main()
{  
debugging_a_bug();
}
